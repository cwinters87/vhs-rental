<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="mr_video.list">
      <field name="name">mr_video list</field>
      <field name="model">mr_video.mr_video</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="mr_video.action_window">
      <field name="name">mr_video window</field>
      <field name="res_model">mr_video.mr_video</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="mr_video.action_server">
      <field name="name">mr_video server</field>
      <field name="model_id" ref="model_mr_video_mr_video"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="mr_video" id="mr_video.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="mr_video.menu_1" parent="mr_video.menu_root"/>
    <menuitem name="Menu 2" id="mr_video.menu_2" parent="mr_video.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="mr_video.menu_1_list" parent="mr_video.menu_1"
              action="mr_video.action_window"/>
    <menuitem name="Server to list" id="mr_video" parent="mr_video.menu_2"
              action="mr_video.action_server"/>
    -->
  </data>
</odoo>